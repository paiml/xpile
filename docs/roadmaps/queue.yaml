# xpile autonomous work-queue — the next-pick source of truth.
#
# PMAT-859 / countermeasure #2 from the capability-vs-contract + stopping audit
# (audit-design.md §6; memory feedback-never-stop-status-updates). The recurring
# failure was "what to do next" being a human-prod-shaped decision, so the loop
# stalled whenever a slice finished. INVARIANT: the loop may end a turn ONLY when
# this queue is empty AND a refill hunt has been launched. Each firing: pop the
# top OPEN item, ship it to full-CI-green + release, mark done, chain the next.
#
# Ordering = EV (frequency x severity), goal-aligned (R6 contract-integrity and
# contract-carrying fidelity ahead of contractless breadth). Pulled from the
# corrected roadmap (memory project-roadmap-goals) + HUNT-V29 (34 confirmed).

updated: 2026-06-22 (V29-1 dict insertion-order VERIFIED+LOCKED → v0.1.584 — ASSESS found the fix already in place, every dict path lowers to indexmap::IndexMap not std HashMap; only the rustc-direct e2e harness blocked verification. The in-tree cargo-test harness links indexmap, so dict_insertion_order_all_paths now pins CPython insertion order end-to-end across views/comprehension/merge; C-XLATE-PY-DICT-TO-HASHMAP tightened (order-independent→insertion-order-preserving). NOT architectural. Prior — V29-2 alias-then-mutate clean-reject stopgap → v0.1.583; full Rc<RefCell> reference layer still architectural; R6-slice3 float-contract still deferred — needs 1 Lean theorem + diamond eq + quorum entry, dedicated slice)
invariant: "never (queue has OPEN items AND main loop idle); a turn ends only on a genuine block or empty-queue+hunt-launched"

queue:
  # ---- R6 contract-integrity (the differentiator; finish before breadth) ----
  - id: R6-slice3
    status: done
    title: "Author C-PY-FLOAT-ARITH contract (Lean theorem + Kani harness + QUORUM, depth-1 Diamond) so float stops emitting uncited; wire float citation in applicable_contracts()"
    tier: R6
    notes: "BIG/multi-file (refinement_proofs.rs + kani_verify.rs iterate all contracts). The depth gate already allows a new contract at depth-1 (assert_grandfathered_floor). Reuse c-int-arith-v1.yaml as template. = R8/PMAT-477 too."
  - id: R6-slice5
    status: done
    title: "Citation-integrity gate part (b): per-fixture EXPECTED-contracts so a contract-bearing construct that MISSES its citation fails (currently only no-phantom + slice-1-live are enforced)"
    tier: R6
  - id: R6-slice4
    status: done
    title: "Author a set type-translation contract (C-XLATE-PY-SET-*) + wire set citation"
    tier: R6

  # ---- HUNT-V29 contract-carrying fidelity (cited+gated by construction) ----
  - id: V29-4
    status: done
    title: "Tuple */+=  lowered as scalar checked_mul/checked_add on a tuple (E0599 / wrong arity); extend try_repeat + lower_aug_assign to element-wise tuple expand"
    severity: silent-wrong/invalid-rust
  - id: V29-5
    status: done
    title: "int/float ternary inside a list-comp silently widens int elements to f64 (prints N.0); CLEAN-REJECT in comp-element position (heterogeneous list out of scope), consistent with the heterogeneous-list-literal reject"
    severity: silent-wrong
  - id: V29-1
    status: done
    title: "dict iteration loses CPython insertion order (HashMap, nondeterministic); needs IndexMap-equivalent. BLOCKED on the rustc-direct e2e harness (no external crate link) — needs a no-dep ordered-map OR harness --extern support. Update C-XLATE-PY-DICT-TO-HASHMAP contract to specify insertion order. RESOLVED v0.1.584 (PMAT-885): ASSESS found the FIX already in place — every dict path lowers to indexmap::IndexMap (insertion-ordered), never std HashMap; only the E2E verification was blocked. The in-tree cargo-test harness (assert_rustc_runs) links the workspace indexmap, so the order is now pinned end-to-end by dict_insertion_order_all_paths (literal .keys()/.values()/.items(), insert+append+update, comprehension, {**a,**b} merge — non-sorted insertion order 3,1,2, asserted vs python3). C-XLATE-PY-DICT-TO-HASHMAP tightened: 'order-independent/HashMap' drift corrected to insertion-order-preserving/IndexMap + FALSIFY-XLATE-PY-DICT-003. NOT architectural after all (no no-dep ordered-map needed)."
    severity: silent-wrong
    tier: architectural
  - id: V29-2
    status: done
    title: "Object reference semantics (alias-as-move E0382; arg-clone drops mutation); needs Rc<RefCell> reference layer + escape/aliasing analysis. Contained stopgap: detect alias-then-mutate and clean-REJECT instead of miscompiling. SHIPPED v0.1.583: arg-clone-drops-mutation case clean-rejected (PMAT-884) — Clone(Ident) arg into a Param::mutable-mutating callee → `object reference semantics not yet supported`. Conservative (false-negative-preferred); full Rc<RefCell> layer + the alias-as-move E0382 case remain architectural (read-only `b=a` aliasing is a missing-clone bug, not a semantic miscompile, so NOT rejected)."
    tier: architectural

  # ---- recurring obligations ----
  - id: cratesio-friday
    status: recurring
    title: "crates.io batch publish (cargo publish --workspace) — LATE Friday only; next 2026-06-26; v0.1.537..current catch-up"
  - id: hunt-refill
    status: refill
    title: "When the OPEN fidelity queue thins, launch a fresh differential hunt (HUNT-V30) to refill — never end on an empty queue without a hunt in flight"
