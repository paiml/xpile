[package]
name = "bashrs-frontend"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "POSIX shell frontend (sh/bash/zsh/mk) for xpile (XPILE-BASHRS-MERGER-001). Parses the flat-command subset plus the full v0.1.0 control-flow surface — nested for/while/until, if/elif/else, top-level case — to meta-HIR. `Makefile`/`Dockerfile`/`*.mk` are ROUTED and then REFUSED: there is no build-driver dialect (PMAT-1420)."

[dependencies]
xpile-frontend = { workspace = true }
xpile-meta-hir = { workspace = true }
