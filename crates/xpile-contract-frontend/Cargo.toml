[package]
name = "xpile-contract-frontend"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Contract frontend trait — proof-lane sibling of Frontend. Parses LaTeX into contract equations; `latex` is the one registered ContractFrontend."

[dependencies]
xpile-contracts = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
