[package]
name = "xpile-contract-backend"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Contract backend trait — proof-lane sibling of Backend. Renders contracts as LaTeX or Lean 4 theorem text; those are the two formats a registered ContractBackend claims."

[dependencies]
xpile-contracts = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
