[package]
name = "xpile"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage = "https://github.com/paiml/xpile"
# PMAT-1487: NOT `https://docs.rs/xpile`. This crate is the ONE member of the 31
# with no `lib` target (`cargo metadata`: kinds `bin`/`custom-build`/`example`/
# `test`), and docs.rs builds documentation by running `cargo rustdoc --lib`. So
# that URL has never rendered a page and structurally cannot: the build log for
# every version says `error: no library targets found in package "xpile"`, and
# docs.rs reports `doc_status:false` for 0.1.615/616/617 while all 30 siblings
# report `true`. THE FAILURE IS NORMAL — ripgrep, fd-find, hyperfine and sd are
# all red on docs.rs for the same reason — WHICH IS WHY NOBODY LOOKED. The defect
# was the CLAIM pointing at it: this key is a hand-written assertion that
# documentation lives at that address, it was written by the 2026-05-15 name-
# reservation commit `ac709bf5` alongside the stub README PMAT-1466 deleted, and
# it is the only `documentation` key in the workspace. Meanwhile the README three
# lines below — the body crates.io renders on this same page — points readers at
# the live mdBook three separate times. The sidebar link and the body disagreed
# and the labelled one was the dead one. The book is deployed from `main` on
# every push by `.github/workflows/book.yml` and measured 200 with real content.
documentation = "https://paiml.github.io/xpile/"
# PMAT-1466: the ROOT README, not a sibling copy. This used to read
# "README.md", resolving to `crates/xpile/README.md` — a bootstrap stub written
# 2026-05-15 for the v0.0.1 name reservation, never touched again (one commit,
# `ac709bf5`), read by no test, and served by crates.io as the flagship's front
# page for 617 releases while it said "Status: v0.0.1 — crates.io name
# reservation. The real CLI lands in v0.1.0+". The stub is deleted rather than
# corrected: two product front pages is what went stale in the first place.
# `../..` packages correctly — `xpile-bigint` resolved to this exact file from
# outside its own directory since the workspace was created.
readme = "../../README.md"
description = "Polyglot transpile workbench: Python/C/Shell/WASM in, Rust/Ruchy/Lean/WASM/PTX/WGSL/SPIR-V/Shell/forjar out, with provable contracts at every layer."
keywords = ["transpile", "polyglot", "compiler", "verification", "rust"]
categories = ["development-tools", "command-line-utilities", "compilers"]
# PMAT-1471: `contracts` is a SYMLINK to the workspace root (PMAT-1407) and
# `cargo package` WALKS it instead of consulting git, so `.gitignore` does not
# apply through it (`git check-ignore` refuses the path outright: "beyond a
# symbolic link"). That swept the UNTRACKED `pv` lint cache under
# `contracts/.pv/` into the uploaded `.crate` — 64 files on this tree, present
# only because the pre-push checklist runs `pv lint contracts/`. The published
# file set therefore depended on the publishing MACHINE: a clean-room package
# has none of them, the operator's has all of them, and nothing compared the
# two. `exclude` is SUBTRACTIVE and safe; `include` is AUTHORITATIVE and would
# re-break PMAT-1407 — see `tests/packaged_contracts.rs` test 3, which bans it.
exclude = ["contracts/.pv/**"]

[[bin]]
name = "xpile"
path = "src/main.rs"

[dependencies]
xpile-core     = { workspace = true }
xpile-backend  = { workspace = true }
# PMAT-1413: `parse_hardware` refuses a malformed `--hardware ptx:<cap>` using
# the codegen crate's own `validate_compute_capability`, so the CLI's grammar
# and the emitter's grammar are ONE function and cannot drift. The
# AUTHORITATIVE refusal stays at the `emit_kernel` choke point (library callers
# never reach `parse_hardware`); this is only the fail-fast wrapper.
# Already in the graph transitively via xpile-core (verified with `cargo tree
# -i`), so this adds no node to the publish DAG — it makes the use direct.
xpile-ptx-codegen = { workspace = true }
xpile-meta-hir = { workspace = true }
# PMAT-897 (Sprint-2 Tier 2): the `xpile hybrid <dir>` command reconciles the
# dispatched modules' FFI boundaries into a manifest (Phase 1 + Phase 2).
xpile-ffi-manifest = { workspace = true }
# PMAT-902 (Sprint Day 3): `xpile hybrid --verify` differential-checks the
# executed hybrid artifact against the CPython reference (ctypes-bound C).
xpile-oracle       = { workspace = true }
# PMAT-1024: `transpile` resolves the target BEFORE lowering and passes the
# target's AliasSemantics to the frontend (reference-semantics targets skip
# the value-semantics alias dispositions).
xpile-frontend     = { workspace = true }
# PMAT-1353: `xpile hybrid --verify --repair` drives the bounded, fail-closed,
# deterministic RepairLoop. Before this the crate held 931 lines and 18 passing
# tests that no user could invoke. OPT-IN ONLY — the default `--verify` output
# and exit code are byte-identical with and without this dependency, which is
# what makes the wiring safe inside a release window.
xpile-agent        = { workspace = true }
anyhow         = { workspace = true }
clap           = { version = "4", features = ["derive"] }

[dev-dependencies]
# PMAT-484: contract_via_roles.rs parses the PTX contract YAML to
# validate the structured compile_targets.via_roles records.
serde_yaml              = { workspace = true }
# PMAT-1350: wasm_contract_surface.rs deserializes the WASM contract's
# `emit_surface` block into typed rows (serde_yaml already depends on serde,
# so this adds no crate to the dependency graph — only the derive).
serde                   = { workspace = true }
# PMAT-475 (R6): diamond_coverage.rs parses the per-contract Diamond
# depth report to grandfather the depth-13 gate (new contracts join at
# depth-1+ without the depth-13 treadmill).
serde_json              = { workspace = true }
# PMAT-270: trait_runtime_properties.rs exercises ContractBackend +
# ContractFrontend traits directly against the live default_session().
xpile-contract-backend  = { workspace = true }
xpile-contract-frontend = { workspace = true }
xpile-contracts         = { workspace = true }
# PMAT-890 (Sprint-2 Tier 1): oracle_differential.rs uses the PythonOracle to
# auto-diff CPython vs transpiled-Rust stdout over self-contained oracle fixtures.
# (xpile-oracle is a regular dependency above — used by main.rs `--verify` too.)
# PMAT-896 (Sprint-2 Tier 2): hybrid_reconcile.rs drives the Python + C frontends
# through FfiManifest::reconcile end-to-end (Phase 1 boundary detection → Phase 2).
# (xpile-ffi-manifest is a regular dependency above — used by main.rs too.)
xpile-frontend          = { workspace = true }
depyler-frontend        = { workspace = true }
decy-frontend           = { workspace = true }
# PMAT-932: hybrid_shell_producer.rs drives the Python + Shell frontends through
# FfiManifest::reconcile to prove the Python→Shell boundary (the `emit_shell_shim`
# producer) reconciles by the shell program name end-to-end.
bashrs-frontend         = { workspace = true }
# PMAT-274: notation_runtime.rs parses notation_demo.tex through the
# real LatexContractFrontend (the test asserts the C-NOTATION three-
# form equivalence claim against on-disk evidence).
latex-contract-frontend = { workspace = true }
# PMAT-1401: wgsl_int_boundary_witness.rs re-validates emitted WGSL through the
# repo's own exported `naga_validate_wgsl` gate, INDEPENDENTLY of the call
# `emit_wgsl_module` makes internally — so `Ok ⟹ naga accepts` stays a checked
# property rather than an unchecked comment if that internal call is ever
# removed. Already in the graph transitively via xpile-backend; this makes the
# test's use of it direct.
xpile-wgsl-codegen      = { workspace = true }
# PMAT-1439: book_api_examples.rs COMPILES the `RustBackend` snippet that
# `book/src/reference/backends.md` publishes, so the book's only backend
# library example is type-checked against the real trait instead of being
# prose. Already in the graph transitively via xpile-core (which registers
# `xpile_rust_codegen::RustBackend` in `default_session`); this makes the
# test's use of it direct and adds no node to the publish DAG.
xpile-rust-codegen      = { workspace = true }
# PMAT-1428: spirv_binary_recoverable_witness.rs recovers the emitted SPIR-V
# module back out of the CLI's text artifact and re-checks it through the
# crate's own `validate_spirv` / `wgsl_to_spirv_words`, so "the artifact
# CONTAINS the module it describes" is a checked property of the emitted text
# rather than a header the emitter writes about itself.
xpile-spirv-codegen     = { workspace = true }
