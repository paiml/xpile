def maxchar(s: str) -> str:
    return max(s)


def minchar(s: str) -> str:
    return min(s)


def chars(s: str) -> list[str]:
    return list(s)


def joined(s: str) -> str:
    return "-".join(list(s))


def char_count(s: str) -> int:
    return len(list(s))
