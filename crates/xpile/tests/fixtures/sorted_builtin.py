# PMAT-502c (Tranche 2): sorted(xs) returns a new sorted list (input unchanged).
def order(xs: list[int]) -> list[int]:
    return sorted(xs)


def order_str(words: list[str]) -> list[str]:
    return sorted(words)
