[package]
name = "bashrs-backend"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "POSIX shell backend (sh/bash/zsh) for xpile (XPILE-BASHRS-MERGER-001). Renders real POSIX shell for the supported meta-HIR Stmt set (Cmd / Pipeline / ShellAssign / ShellLoop with quoting, variables, and command substitution); a ShellCheck-compatible verifier remains v0.2.0 work."

[dependencies]
xpile-backend = { workspace = true }
xpile-contracts = { workspace = true }
xpile-meta-hir = { workspace = true }
